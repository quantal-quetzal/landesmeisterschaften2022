<!DOCTYPE html>
<html class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <title>{{ title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" type="text/css" media="screen" href="/main.css" />
  </head>
  <body class="m-0 p-0 flex flex-col bg-gray-200 min-h-full">
    <div class="bg-white shadow-md">
      <h1 class="text-2xl p-4 font-bold text-red-500">
        Landesmeister&shy;schaften 2022
      </h1>
      <nav class="flex flex-wrap gap-2 p-2 pt-0">
        {% for section in collections.section %}
          <div>
            <a href="{{ section.url }}"">
              <button
                class="font-semibold py-2 px-2 border border-red-500 hover:border-transparent rounded
                {% if page.url == section.url %} 
                  hover:bg-red-600
                  bg-red-500 
                  text-white
                {% else %}
                  bg-transparent
                  hover:bg-red-600
                  text-red-500
                  hover:text-white
                {% endif %}
                "
              >
                {{ section.data.title }}
              </button>
            </a>
          </div>
        {% endfor %}
      </nav>
    </div>
    <main class="flex-1 h-full">{{ content }}</main>
    <footer class="bg-white shadow-xl p-4 text-center">
      <p class="text-gray-500">Viel Spaß wünscht euch das Organisationsteam der Ortsgruppe Neuenkirchen/Wettringen.</p>
      <p class="pt-2">Created with ❤️, ☕ and <span class="text-white font-mono bg-black font-bold p-1 pt-2 text-xs"><a href="https://www.11ty.dev/">11ty</a></span> by <a class="underline" href="https://felix-gehring.de">Felix</a>.</p>
    </footer>
  </body>
</html>
